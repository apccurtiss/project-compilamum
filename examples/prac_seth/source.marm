import frontend "document.getElementById(id).innerHTML = value;" as change_element(id:string, value:Int);

frontend function get_counter() -> Void {
    int x = handle_counter();
    change_element("p1",x)
}

backend int i = 0;

backend function handle_counter() -> Int {
    i = i + 1;
    return i;
}
