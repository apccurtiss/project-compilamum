frontend javascript function change_element(value: Int) {
    document.getElementById("p1").innerHTML = value;
}

frontend function get_counter() -> Int {
    let x: Int = handle_counter();
    change_element(x)
}

let backend i: Int = 0;

backend function handle_counter() -> Int {
    i = i + 1;
    return i;
}
