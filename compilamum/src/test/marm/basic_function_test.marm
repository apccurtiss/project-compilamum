frontend submit(): Number {
  x = foo(1,2);
  y = foo(1,2);
  z = foo(x,y);
  print(z);
  return z;
}

frontend printit(): Void {
  y = submit();
  print(y);
}

backend foo(x: Number, y: Number): Number {
  print(x);
  print(y);
  return x + y;
}
